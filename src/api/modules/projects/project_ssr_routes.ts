import { IApp } from "@src/api/fw/api_app_types.ts";

export class ProjectSsrRoutes {
	constructor(_app: IApp) {}

	register_routes() {
		// Project SSR routes are auto-registered in src/api/route_registry.ts.
	}
}
