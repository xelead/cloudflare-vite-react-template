# People SSR Action Handlers

The `src/api/modules/people/ssr_actions` folder contains SSR route entry files
plus a shared `people_ssr_handlers.ts` factory output. Route files define only
path metadata and reuse centralized handler logic.
