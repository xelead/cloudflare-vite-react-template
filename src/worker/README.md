# Backend API Worker

This worker module provides the backend API routes for handling project-related operations. It includes both API routes for server-side rendering (SSR) and API routes for handling API requests.
