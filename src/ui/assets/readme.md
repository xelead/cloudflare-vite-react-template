# UI Static Assets

The `src/ui/assets` folder stores static visual assets used by the React UI,
such as SVG logos and imagery. This folder keeps media resources centralized so
components can import them consistently.

## Agent Instructions

Place only static, UI-consumable assets here and keep names descriptive and
stable for imports. Optimize asset size and format where possible, and avoid
mixing generated or runtime data files into this folder.
