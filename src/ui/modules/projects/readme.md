# Projects React Module

Contains the `Projects` list page, dynamic project details page (`/projects/:project_id`), project edit page (`/projects/:project_id/edit`), local data context, and response types used with API-provided data. The list page also supports delete confirmation via dialog.
